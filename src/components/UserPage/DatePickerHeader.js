// const selectYear = document.querySelectorAll('select#years');

// const range = new Range();

// range.setStart(selectYear.firstChild, 1955);

// range.setEnd(selectYear.lastChild, getYear(new Date()) + 1);

// const years = window.getSelection();

// years.addRange(range);

// console.log(selectYear);

// const range = new Range();

// range.setStart(selectYear.firstChild, 1955);

// range.setEnd(selectYear.lastChild, getYear(new Date()) + 1);

// const years = window.getSelection();

// years.addRange(range);

// const getEndYear = date => {
//   const dateYear = date.getFullYear();
//   let rangeStart = dateYear;
//   let count = 0;
//   while (!Number.isInteger((dateYear - count) / 12)) {
//     count++;
//   }
//   rangeStart = dateYear - count + 1;
//   return [rangeStart, rangeStart + 12];
// };
